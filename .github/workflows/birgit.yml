name: Build and Publish

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  docker:
    runs-on: ubuntu-latest
       
    steps:
      - uses: actions/checkout@v1
      - name: script
        run: test.sh
        shell: bash

name: Build and Publish

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  docker:
    runs-on: ubuntu-latest
       
    steps:
      # gets the latest repo
      - uses: actions/checkout@v3
      - name: run the script
        run: ls -la
